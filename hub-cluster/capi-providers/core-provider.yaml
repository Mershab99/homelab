apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: CoreProvider
metadata:
  name: cluster-api
spec:
  version: v1.9.0
  deployment:
    spec:
      template:
        spec:
          nodeSelector:
            node-role.kubernetes.io/agent: ""
