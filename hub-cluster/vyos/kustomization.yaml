apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sync-cronjob.yaml
configMapGenerator:
  - name: vyos-config
    files:
      - config.boot
generatorOptions:
  disableNameSuffixHash: true
