apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Phase 7+ — Sveltos cross-cluster boundary:
#   cluster-profiles, event-sources, event-triggers, templates, cluster-classes.
resources: []
