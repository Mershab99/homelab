# netbird — self-hosted NetBird SERVER on the HUB (contraxia). The private-access
# backbone. Moved off arrakis 2026-08-05 (Dex + netbird both live on the hub now).
#
# STAGE 1 (this file): management + dashboard + signal, OIDC federated to the hub
# Dex (auth.mershab.com, live). Exposed via the hub nginx edge (02c → chisel → DO
# droplet). Relay deferred to S1b; operator + routing-peer (API-over-mesh) to S2/S3.
#
# Secrets: netbird-management-secrets in THIS (netbird) ns — NETBIRD_DATASTORE_ENC_KEY
# (management store encryption). Dex has the `netbird-client` public/PKCE+device client.
#
# Selector: persona=infra.
apiVersion: config.projectsveltos.io/v1beta1
kind: ClusterProfile
metadata:
  name: netbird
spec:
  clusterSelector:
    matchLabels:
      persona: infra
  dependsOn:
    - auth-stack          # Dex (auth.mershab.com) must exist to federate OIDC
    - ingress-nginx-hub   # the hub nginx edge (+ DO LB) fronts netbird/signal.mershab.com
  helmCharts:
    - repositoryURL: https://netbirdio.github.io/helms
      repositoryName: netbird
      chartName: netbird/netbird
      chartVersion: 1.9.0            # app 0.46.0; PIN EXACT (repo rule)
      releaseName: netbird
      releaseNamespace: netbird
      helmChartAction: Install
      values: |
        management:
          enabled: true
          env:
            NB_SETUP_PAT_ENABLED: "true"
          # --config points at the WRITABLE PVC (/var/lib/netbird) so netbird can
          # persist its generated store-encryption key. The chart mounts
          # management.json from a READ-ONLY ConfigMap at /etc/netbird; the
          # seed-config initContainer (spec.patches below) copies it onto the PVC
          # once (cp-if-absent). No secret in git — netbird generates + persists
          # the key on the PVC itself; it survives restarts (PVC is persistent).
          podCommand:
            args:
              - --port=80
              - --log-file=console
              - --log-level=info
              - --disable-anonymous-metrics=false
              - --single-account-mode-domain=netbird.selfhosted
              - --dns-domain=netbird.selfhosted
              - --config=/var/lib/netbird/management.json
          # management.json — OIDC → hub Dex. Relay/TURN/Stun omitted in S1; peers
          # register + connect P2P via signal.
          configmap: |
            {
              "Signal": {"Proto":"https","URI":"signal.mershab.com:443"},
              "HttpConfig": {
                "AuthAudience": "netbird-client",
                "AuthIssuer": "https://auth.mershab.com",
                "AuthKeysLocation": "https://auth.mershab.com/keys",
                "AuthUserIDClaim": "sub",
                "OIDCConfigEndpoint": "https://auth.mershab.com/.well-known/openid-configuration"
              },
              "DeviceAuthorizationFlow": {
                "Provider": "hosted",
                "ProviderConfig": {
                  "Audience": "netbird-client",
                  "ClientID": "netbird-client",
                  "Domain": "auth.mershab.com",
                  "Scope": "openid profile email offline_access api"
                }
              },
              "PKCEAuthorizationFlow": {
                "ProviderConfig": {
                  "Audience": "netbird-client",
                  "ClientID": "netbird-client",
                  "Scope": "openid profile email offline_access api",
                  "RedirectURLs": ["https://netbird.mershab.com/peers","https://netbird.mershab.com/callback","https://netbird.mershab.com/silent-auth"]
                }
              }
            }
          # HTTP API (dashboard + REST) on netbird.mershab.com/api.
          ingress:
            enabled: true
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
            hosts:
              - host: netbird.mershab.com
                paths:
                  - path: /api
                    pathType: Prefix
            tls:
              - secretName: netbird-mershab-tls
                hosts: [netbird.mershab.com]
          # Management gRPC (HTTP/2) on netbird.mershab.com — nginx needs the GRPC
          # backend protocol (chart's ingressGrpc sets it).
          ingressGrpc:
            enabled: true
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
              nginx.ingress.kubernetes.io/backend-protocol: GRPC
            hosts:
              - host: netbird.mershab.com
                paths:
                  - path: /management.ManagementService
                    pathType: Prefix
            tls:
              - secretName: netbird-mershab-tls
                hosts: [netbird.mershab.com]
        dashboard:
          enabled: true
          env:
            AUTH_AUTHORITY: https://auth.mershab.com
            AUTH_CLIENT_ID: netbird-client
            AUTH_AUDIENCE: netbird-client
            AUTH_SUPPORTED_SCOPES: openid profile email offline_access api
            USE_AUTH0: "false"
            AUTH_REDIRECT_URI: /peers
            AUTH_SILENT_REDIRECT_URI: /silent-auth
            NETBIRD_MGMT_API_ENDPOINT: https://netbird.mershab.com
            NETBIRD_MGMT_GRPC_API_ENDPOINT: https://netbird.mershab.com
          # SPA at the root of netbird.mershab.com.
          ingress:
            enabled: true
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
            hosts:
              - host: netbird.mershab.com
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: netbird-mershab-tls
                hosts: [netbird.mershab.com]
        signal:
          enabled: true
          # signal gRPC on signal.mershab.com (chart key is `ingress`, not
          # ingressGrpc — signal has no separate gRPC ingress; set GRPC here).
          ingress:
            enabled: true
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
              nginx.ingress.kubernetes.io/backend-protocol: GRPC
            hosts:
              - host: signal.mershab.com
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: signal-mershab-tls
                hosts: [signal.mershab.com]
        relay:
          enabled: false   # S1b — needs NETBIRD_RELAY_AUTH_SECRET wired
  # Make management.json writable without a git secret: seed the read-only
  # ConfigMap copy onto the writable PVC (/var/lib/netbird) ONCE, so netbird can
  # generate + persist its store-encryption key there (--config points at the PVC).
  # cp-if-absent → netbird's persisted key survives restarts; a config change needs
  # a one-time PVC reset (delete /var/lib/netbird/management.json).
  patches:
    - target:
        group: apps
        version: v1
        kind: Deployment
        name: netbird-management
      patch: |
        apiVersion: apps/v1
        kind: Deployment
        metadata:
          name: netbird-management
        spec:
          template:
            spec:
              initContainers:
                - name: seed-config
                  image: busybox:1.37
                  command:
                    - sh
                    - -c
                    - "[ -f /var/lib/netbird/management.json ] || cp /etc/netbird/management.json /var/lib/netbird/management.json"
                  volumeMounts:
                    - {mountPath: /etc/netbird, name: config}
                    - {mountPath: /var/lib/netbird, name: management}
