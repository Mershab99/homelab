apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Phase 1+ — populated with operator HelmReleases:
#   cilium, multus, whereabouts, cert-manager, capi-operator, kamaji, sveltos,
#   kubevirt-hco, vrouter-operator, dex, oauth2-proxy, velero, rook-operator,
#   otel-operator, prom-operator, grafana-operator, loki-operator, tempo-operator.
resources: []
