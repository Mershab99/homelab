# TEMPLATE — Coder's Dex OIDC client secret. Must MATCH the `coder` staticClient
# secret in dex-config (secrets/infrastructure/dex/README.md). Copy to
# coder-oidc.secret.yaml, fill, `./secrets/apply.sh` AGAINST THE TENANT.
# *.secret.yaml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: coder-oidc
  namespace: coder
type: Opaque
stringData:
  client-secret: REPLACE_WITH_CODER_DEX_CLIENT_SECRET
