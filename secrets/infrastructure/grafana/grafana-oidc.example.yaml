# TEMPLATE — copy to grafana-oidc.secret.yaml, fill clientSecret (must equal
# the `grafana` staticClient secret in dex-config), then `./secrets/apply.sh`.
# *.secret.yaml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-oidc
  namespace: monitoring
type: Opaque
stringData:
  clientSecret: REPLACE_WITH_GRAFANA_CLIENT_SECRET
