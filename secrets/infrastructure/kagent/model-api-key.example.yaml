# TEMPLATE — model API key for kagent ModelConfig. Apply INSIDE the `ai` vCluster
# (use the ai vcluster kubeconfig, NOT the tenant). Copy to
# model-api-key.secret.yaml, fill, and apply. *.secret.yaml is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: kagent-model-key
  namespace: kagent
type: Opaque
stringData:
  API_KEY: REPLACE_WITH_MODEL_PROVIDER_API_KEY
