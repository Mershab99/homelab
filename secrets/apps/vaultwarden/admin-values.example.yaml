# Helm values fragment merged into the vaultwarden chart by Sveltos
# (18-mershab-apps `valuesFrom`). Lives on MGMT (ns tenant-secrets).
# Generate: `openssl rand -base64 48` (or vaultwarden's argon2 PHC string —
# see https://github.com/dani-garcia/vaultwarden/wiki/Enabling-admin-page).
apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-admin-values
  namespace: tenant-secrets
type: Opaque
stringData:
  values.yaml: |
    env:
      ADMIN_TOKEN: REPLACE_WITH_VAULTWARDEN_ADMIN_TOKEN
