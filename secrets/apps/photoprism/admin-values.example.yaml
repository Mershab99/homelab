# Helm values fragment merged into the photoprism chart by Sveltos
# (17-family-apps `valuesFrom`). Lives on MGMT (ns tenant-secrets). ROTATE —
# the old admin password was committed to the previous repo and is burned.
apiVersion: v1
kind: Secret
metadata:
  name: photoprism-admin-values
  namespace: tenant-secrets
type: Opaque
stringData:
  values.yaml: |
    env:
      PHOTOPRISM_ADMIN_PASSWORD: REPLACE_WITH_PHOTOPRISM_ADMIN_PASSWORD
