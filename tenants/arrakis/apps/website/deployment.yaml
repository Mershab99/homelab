# nginx serving the mershab.com static site from the website-content ConfigMap.
# Flat `web` namespace on arrakis (collapsed out of the mershab k3k vCluster).
apiVersion: v1
kind: Namespace
metadata:
  name: web
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: website
  namespace: web
  labels: {app: website}
spec:
  replicas: 2
  selector:
    matchLabels: {app: website}
  template:
    metadata:
      labels: {app: website}
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - {containerPort: 80, name: http}
          volumeMounts:
            - {name: content, mountPath: /usr/share/nginx/html, readOnly: true}
          readinessProbe:
            httpGet: {path: /, port: 80}
          resources:
            requests: {cpu: 10m, memory: 16Mi}
            limits: {memory: 64Mi}
      volumes:
        - name: content
          configMap: {name: website-content}
